#include "fs/operations.h"
#include <assert.h>
#include <stdio.h>
#include <string.h>
#include <errno.h>
#include <pthread.h>




int main(){
	pthread_t tid[3];

	/*TODO*/
	/*Preciso de ajuda aqui....*/
	/*
	if (pthread_create(&tid[0], NULL, ,) != 0) {
	}


    pthread_join(tid[0], NULL);
    pthread_join(tid[1], NULL);
	pthread_join(tid[2, NULL])
	*/

}
